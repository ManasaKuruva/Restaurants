# Spring Datasource Configuration
#spring.datasource.url=${DB_URL} # Set your database URL as an environment variable
#spring.datasource.username=${DB_USERNAME} # Set your database username as an environment variable
#spring.datasource.password=${DB_PASSWORD} # Set your database password as an environment variable
spring.datasource.url=jdbc:mysql://localhost:3306/restaurant_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=yourpassword
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Server Configuration
server.port=8080
server.servlet.context-path=/api

# Logging Configuration
logging.level.org.springframework=INFO
logging.level.com.example=DEBUG

# Profiles
#spring.profiles.active=dev
# JWT Configuration
jwt.secret=mySuperSecretKey
spring.profiles.active=prod

